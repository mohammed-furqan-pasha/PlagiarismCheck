import React from 'react';
import App from '../App';

// In a real application, this file would typically contain the main logic/layout.
// Since we put all the logic into App.jsx for the prototype, this acts as a wrapper.

function Home() {
    return (
        <App />
    );
}

export default Home;
